type: object
required:
  - id
  - description
properties:
  id:
    type: string
    format: string
    description: The claim unique identifier
    example: KYCCredential
  description:
    type: string
    format: string
    description: The claim description
    example: This claim proves that you go through the KYC procedure
