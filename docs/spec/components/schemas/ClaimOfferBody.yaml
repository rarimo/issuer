type: object
required:
  - url
  - credentials
properties:
  url:
    type: string
    format: string
    description: The callback url
    example: 'https://issuer.com/integrations/issuer/v1/claims/issue'
  credentials:
    type: array
    items:
      $ref: '#/components/schemas/ClaimOfferBodyCredentials'
